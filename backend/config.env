PORT=4000
JWT_SECRET=factura_sqlite_secret_key_2024_change_in_production
NODE_ENV=production
# Ruta de la base de datos SQLite (relativa al directorio del backend)
# En produccion Electron, se usara app.getPath('userData')
SQLITE_DB_PATH=./data/factura.db
