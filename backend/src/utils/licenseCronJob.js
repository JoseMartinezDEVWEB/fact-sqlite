// Cron job de licencias simplificado para SQLite
export function scheduleLicenseCheck() {
  // No es necesario con SQLite - sin servidor de licencias externo
  console.log('License check scheduled (SQLite mode - no remote check needed)');
}
